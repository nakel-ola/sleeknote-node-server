import { Request, Response } from "express";

const getProducts = (req: Response, res: Request) => {};
const getProductsBySlug = (req: Response, res: Request) => {};
const getProductsById = (req: Response, res: Request) => {};

export { getProducts, getProductsById, getProductsBySlug };
